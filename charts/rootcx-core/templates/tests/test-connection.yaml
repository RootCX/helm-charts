apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rootcx-core.fullname" . }}-test-connection"
  labels:
    {{- include "rootcx-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '--timeout=5', '{{ include "rootcx-core.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
