RootCX Core has been deployed.

{{- if .Values.ingress.enabled }}
Access the application at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else if .Values.route.enabled }}
Access the application at:
  https://{{ .Values.route.host }}
{{- else }}
Get the application URL by running:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "rootcx-core.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 9100:9100
  echo "Visit http://127.0.0.1:9100"
{{- end }}

{{- if .Values.postgresql.enabled }}

PostgreSQL is running as part of this release (with PGMQ and pg_cron extensions).
{{- if .Values.route.enabled }}

OPENSHIFT: The PostgreSQL init container requires the 'anyuid' SCC.
If pods are not starting, run:
  oc adm policy add-scc-to-user anyuid -z {{ include "rootcx-core.serviceAccountName" . }} -n {{ .Release.Namespace }}
{{- end }}
{{- else }}
Using external database: {{ .Values.databaseUrl }}
{{- end }}

{{- if .Values.oidc.issuer }}

SSO/OIDC is configured with issuer: {{ .Values.oidc.issuer }}
{{- end }}
