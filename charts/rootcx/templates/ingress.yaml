{{- $managedHost := dig "hosts" "portal" "" .Values.global.rootcx -}}
{{- $tlsSecret := dig "tls" "secretName" "" .Values.global.rootcx -}}
{{- $managed := and (eq (include "rootcx-portal.platform" .) "kubernetes") $managedHost -}}
{{- if or .Values.ingress.enabled $managed -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "rootcx-portal.fullname" . }}
  labels:
    {{- include "rootcx-portal.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if and $managedHost $tlsSecret }}
  tls:
    - hosts:
        - {{ $managedHost | quote }}
      secretName: {{ $tlsSecret | quote }}
  {{- else if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- if $managedHost }}
    - host: {{ $managedHost | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "rootcx-portal.fullname" . }}
                port:
                  name: http
    {{- else }}
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "rootcx-portal.fullname" $ }}
                port:
                  name: http
          {{- end }}
    {{- end }}
    {{- end }}
{{- end }}
