apiVersion: v1
kind: Service
metadata:
  name: {{ include "rootcx-portal.fullname" . }}
  labels:
    {{- include "rootcx-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rootcx-portal.selectorLabels" . | nindent 4 }}
